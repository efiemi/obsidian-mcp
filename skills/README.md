# Obsidian MCP Skills Catalog

This directory contains one Agent Skill per MCP tool exposed by this repository.

Each skill follows the agentskills.io specification:
- one directory per skill
- one SKILL.md with YAML frontmatter and markdown body
- names are lowercase kebab-case and match directory names
